## Research Depth & Scoring Accuracy — Fixes and Improvements

Baseline: ~18 API calls per analysis run.

| ID | Type | Problem | Solution | Impact | Extra calls | Cost +% | Priority |
|----|------|---------|----------|--------|-------------|---------|----------|
| RA-01 | fix | Phase 3 calls `callAnalystAPI` with only 3 args (`useAnalysis.js:2359, 2400, 2441`) — the 4th arg `{ liveSearch, includeMeta }` is missing. Phase 3 never triggers web search in `api/analyst.js`, and no metadata (webSearchCalls) is tracked. The prompt tells the analyst to "defend with NEW evidence not previously cited" but the model has no web access — defense is limited to restating model memory from Phase 1. Same issue on the consistency check call at line 2441 and the condensed retry at line 2400. | Pass `{ liveSearch: true, includeMeta: true }` as the 4th argument to all three `callAnalystAPI` calls in the Phase 3 block. Add `absorbAnalystMeta(analysisMeta, res.meta)` after each call to track Phase 3 web search usage in `analysisMeta`. The existing `api/analyst.js` already handles `liveSearch` correctly — this is purely a frontend orchestration fix. **Files:** `src/hooks/useAnalysis.js` lines 2359, 2400, 2441. | Analyst can actually find new sources to defend scores. e.g. critic says "Mastercard claim is stale" → analyst can now search for fresh data instead of just restating Phase 1 text. | 0 | 0% | P0 |
| RA-02 | fix | `buildCriticPrompt` (`useAnalysis.js:452`) clips the analyst's `full` field to 320 chars: `clip(dim.full, 320)`. The `full` field is typically 500-1200 chars (3-5 paragraphs). The critic sees only the first paragraph. Specific metrics, named deployments, dollar figures, and dates usually appear in paragraphs 2-4. Combined with `brief` (190 chars), the critic gets ~510 chars total per dimension — it literally cannot see the most auditable claims. | Increase `clip(dim.full, 320)` to `clip(dim.full, 800)` in the `evidenceSnapshots` builder inside `buildCriticPrompt`. Also increase `clip(dim.brief, 190)` to `clip(dim.brief, 260)`. This adds ~5000 tokens to the critic prompt (11 dims × ~480 extra chars) but gives the critic the actual evidence to audit. The `api/critic.js` uses `gpt-5.4` which handles large context. The critic `maxTokens: 6000` for the response is already sufficient. **Files:** `src/hooks/useAnalysis.js` line 452-453 in `buildCriticPrompt`. | Critic can actually audit real claims. e.g. analyst cites "$50M savings" in paragraph 3 → critic currently can't see it and never challenges it. | 0 | 0% | P0 |
| RA-03 | fix | `enforcePhase3DecisionRules` (`useAnalysis.js:1363-1435`) validates concessions thoroughly (requires explicit `decision: "concede"`, specific `revisionJustification` via `hasSpecificRevisionReason`, non-"none" `revisionBasis` — reverts if any check fails). But defenses bypass ALL checks — line 1419-1425 just sets `decision: "defend"` and `revisionBasis: "none"` with no validation. No check for new evidence, no source count, no confidence floor. Combined with RA-01 (no web search), the analyst always wins by restating Phase 1 reasoning. | Two-part fix. **Prompt-level:** In Phase 3 prompt (~line 2297), change defense instruction to: "defend your score by citing at least ONE new named source (company, report, deployment) not cited in Phase 1. A defense with zero new sources is weak — if no new source is available, downgrade confidence to medium (or low if already medium) and state what source would strengthen the defense." **Code-level (optional):** In `enforcePhase3DecisionRules`, for defended dims, count Phase 3 sources not in Phase 1 sources (compare by `source.name`). If zero new sources AND confidence "high", downgrade to "medium" and log an adjustment — mirroring the existing concession symmetry. **Files:** `src/hooks/useAnalysis.js` Phase 3 prompt ~line 2297, `enforcePhase3DecisionRules` ~line 1363. | Debate becomes meaningful. e.g. analyst scores ROI 4/5, critic disagrees, analyst just restates "strong ROI" → now must cite a new source or admit lower confidence. | 0 | 0% | P0 |
| RA-04 | fix | `normalizeLowConfidenceSearchHarvest` (`useAnalysis.js:633`) fallback coverage builder uses `useful: findings.some((f) => !f.query \|\| f.query === q)`. The `!f.query` clause means any finding with an empty/missing `query` field marks ALL queries as useful. If the model returns one finding with `query: ""`, every coverage entry gets `useful: true`. Downstream, `usefulQueryCount` (line 1995) is inflated, preventing research briefs from appearing for dimensions that genuinely need them. | Change `!f.query \|\| f.query === q` to `f.query && f.query === q` at line 633. Findings with empty query strings become unmatched and don't inflate any query's useful count. **Files:** `src/hooks/useAnalysis.js` line 633 in `normalizeLowConfidenceSearchHarvest`. | Research briefs appear when search actually fails. e.g. 3 of 4 queries found nothing but system reports "all useful" → PM never sees the evidence gap. | 0 | 0% | P1 |
| RA-05 | fix | `applyConsistencyAdjustments` (`useAnalysis.js:1192-1217`) changes `finalScore` and sets `scoreChanged: true`, but does NOT set `decision: "concede"`, `revisionBasis`, or `revisionJustification`. When `enforcePhase3DecisionRules` runs immediately after (line 2458-2462), it sees a score change without proper concession fields and reverts it to the original score (line 1396-1409). The consistency check runs but its corrections are silently undone. | In `applyConsistencyAdjustments` (line 1209-1214), when a score changes, also write the fields `enforcePhase3DecisionRules` expects: `decision: "concede"`, `revisionBasis: "rubric_alignment"`, `revisionJustification: audit?.dimensions?.[d.id]?.reason \|\| "Adjusted for rubric consistency after cross-phase audit."`. **Files:** `src/hooks/useAnalysis.js` line 1209-1214 in `applyConsistencyAdjustments`. | Rubric-inconsistent scores get corrected. e.g. consistency audit catches inverted regulatory score → currently the fix is silently undone by the guard. | 0 | 0% | P1 |
| RA-06 | fix | `buildHybridReconcileEvidencePrompt` (`useAnalysis.js:391-443`) passes per-dimension snapshots with score, brief (180 chars), full (320 chars), sources, confidence, confidenceReason — but does NOT include `missingEvidence` from either pass. The model merging evidence has no way to know that baseline flagged "no independent audit available" while web found one, or that web flagged a gap that baseline filled. Merge decisions are made on text similarity alone. | Add `missingEvidence` to the per-dimension comparison block (lines 395-410): `BASELINE missing evidence: "${clip(b.missingEvidence, 160)}"` and same for web. Add reconciliation rules to the prompt: "When confidence differs between drafts, prefer evidence from the higher-confidence draft. When both flag the same missingEvidence, preserve it. When one draft fills a gap the other flagged, include the gap-filling evidence." **Files:** `src/hooks/useAnalysis.js` lines 395-410 in `buildHybridReconcileEvidencePrompt`. | Merge favors the more confident pass. e.g. baseline ROI=4 (high conf) vs web ROI=2 (low conf) → reconciliation currently treats them equally. | 0 | 0% | P1 |
| RA-07 | fix | `absorbLowConfidenceMeta` (`useAnalysis.js:1592-1599`) calls `absorbAnalystMeta(analysisMeta, meta)` first (which increments `analysisMeta.webSearchCalls`), then also increments `analysisMeta.lowConfidenceTargetedWebSearchCalls`. A single low-confidence search with 3 web calls shows as `webSearchCalls: +3` AND `lowConfidenceTargetedWebSearchCalls: +3` — total appears as 6 but only 3 calls were made. Also sets `liveSearchUsed` on the main analyst meta even if only the targeted cycle used live search. | Remove the `absorbAnalystMeta(analysisMeta, meta)` call from `absorbLowConfidenceMeta`. Track only in targeted fields: `lowConfidenceTargetedSearchUsed`, `lowConfidenceTargetedWebSearchCalls`, `lowConfidenceTargetedFallbackReason`. **Files:** `src/hooks/useAnalysis.js` line 1592-1599, function `absorbLowConfidenceMeta`. | Cost estimator FR will show correct totals. e.g. 3 targeted searches currently counted as 6 (3 main + 3 targeted). | 0 | 0% | P2 |
| RA-08 | improvement | `buildRubricCalibrationBlock` (`rubric.js:48-56`) only extracts Score 5 and Score 1 anchors using `extractScoreLine`. This block is included in critic prompt (line 481) and Phase 3 prompt (line 2289). Without Score 2/3/4 anchors, models must interpolate the threshold between adjacent scores — and do so inconsistently between runs. The `extractScoreLine` helper already supports any score number. Some dimensions in `dimensions.js` have Score 2/4 text in `fullDef`, others don't. | 1. In `dimensions.js`: audit all 11 dimensions, add Score 2 and Score 4 paragraphs to any `fullDef` missing them (same pattern as existing anchors: one sentence with a concrete threshold). 2. In `rubric.js`: update `buildRubricCalibrationBlock` to emit all 5 levels: `5: ${s5} \| 4: ${s4} \| 3: ${s3} \| 2: ${s2} \| 1: ${s1}`. 3. Also update `buildDimRubricReminder` (line 33-46) to include Score 4 and 2 — used in follow-up prompts. **Files:** `src/constants/dimensions.js`, `src/lib/rubric.js`. | Same use case scored twice gets consistent results. e.g. "insurance claims AI" might get feasibility 3 one run and 4 the next — anchors reduce this variance. | 0 | 0% | P1 |
| RA-09 | improvement | `buildPhase1EvidencePrompt` (`useAnalysis.js:151-192`) tells the analyst to cite "REAL named companies with SPECIFIC metrics" but doesn't distinguish source credibility. A vendor blog saying "our clients save 40%" is treated the same as an independent audit confirming $50M savings. This inflates roi (18%), evidence (13%), ai_fit (14%) — 45% of total weight. The evidence JSON template (`buildDimEvidenceJsonTemplate`, line 54-72) has no `sourceType` field. | 1. Add SOURCE CREDIBILITY RULE to `buildPhase1EvidencePrompt` rules (line 179-182): classify each source as `vendor` (vendor blog, product page, self-reported), `press` (news, earnings call, regulatory filing), or `independent` (peer-reviewed, benchmark, audit). Vendor-only dimensions must flag in `missingEvidence`. 2. Add `"sourceType": "<vendor\|press\|independent>"` to the source object in `buildDimEvidenceJsonTemplate`. 3. Pass `sourceType` through `sanitizeEvidenceItem` (line 1510-1526). **Files:** `src/hooks/useAnalysis.js` lines 151-192, 54-72, 1510-1526. | Inflated scores from marketing claims get caught. e.g. "UiPath claims 80% cost reduction" → flagged as vendor-only, needs independent corroboration. | 0 | 0% | P1 |
| RA-10 | improvement | `buildCriticPrompt` (`useAnalysis.js:445-500`) reviews each dimension independently — no cross-dimension coherence check. Structurally suspicious combinations (evidence=4 + roi=2, ai_fit=4 + feasibility=2) pass unchallenged because the critic never compares dimensions against each other. | Add a CROSS-DIMENSION COHERENCE CHECK block to `buildCriticPrompt`, after the rubric calibration (line 481): flag 5 specific incoherent pairs — `evidence>=4 AND roi<=2`, `ai_fit>=4 AND feasibility<=2`, `build_vs_buy<=2 AND market_size>=4`, `evidence<=2 AND roi>=4`, `ttv>=4 AND change_mgmt<=2`. Instruct critic to note incoherent pairs in the relevant dimension's `critique` field. Adds ~80 tokens. **Files:** `src/hooks/useAnalysis.js` in `buildCriticPrompt`, after line 481. | Contradictory scores get caught. e.g. evidence=4 but roi=2 — strong evidence usually implies measurable financial outcomes. | 0 | 0% | P1 |
| RA-11 | improvement | `regulatory`, `change_mgmt`, `build_vs_buy` have counterintuitive polarity (higher = less burden/disruption/SaaS pressure). `getPolarityHint()` in `rubric.js` provides correct text but it's mixed in with 11 dimensions. Models still invert ~15% of runs — e.g. "healthcare is regulated" → score 1/5 when rubric says 1 = heavy burden, not "industry is regulated." | Add explicit "do NOT" polarity warnings in two prompt locations: 1. Phase 3 prompt (after rubric calibration, ~line 2289): "regulatory: Score 5 = LOW regulatory burden. Do NOT score 1 merely because the industry is regulated. Score actual delivery burden." Same pattern for change_mgmt and build_vs_buy. 2. Phase 1 scoring prompt (`buildPhase1ScoringPrompt`, ~line 209): same warning block after the rubric. **Files:** `src/hooks/useAnalysis.js` Phase 3 prompt ~line 2289, `buildPhase1ScoringPrompt` ~line 209. | Fewer wrong-direction scores. e.g. "healthcare is regulated" → model scores regulatory 1/5 when it should score based on actual delivery burden (maybe 3/5). | 0 | 0% | P1 |
| RA-12 | improvement | `calcWeightedScore` (`scoring.js:28-38`) does `wSum += sc * d.weight` — confidence is ignored. A Score 4 with `low` confidence (model admits "fewer than two verifiable deployments, heavy extrapolation") contributes identically to Score 4 with `high` confidence. Rankings are misleading when comparing use cases with different confidence profiles. | Add `calcConfidenceWeightedScore()` alongside existing function. Use discount factors: `{ high: 1.0, medium: 0.85, low: 0.65 }`. For each dimension, multiply `score * weight * discount`. Get confidence from `uc.finalScores?.dimensions?.[d.id]?.confidence` falling back to `uc.dimScores?.[d.id]?.confidence`, normalized via `normalizeConfidenceLevel` from `lib/confidence.js`. Show both raw and adjusted scores in UI. **Files:** `src/lib/scoring.js`, `src/components/TotalPill.jsx`, `src/components/OverviewTab.jsx`. | Rankings reflect certainty. e.g. Use case A: 72% all-high-confidence ranks above Use case B: 74% with 3 low-confidence dims → adjusted to 68%. | 0 | 0% | P1 |
| RA-13 | improvement | Low-confidence targeted cycle (`useAnalysis.js:1825-2093`) runs once per dimension: query plan → search harvest → rescore. System tracks `usefulQueryCount` (line 1995) and `unsuccessfulQueries` (line 1996-2000) but doesn't act on them. If initial queries are too broad, the entire cycle produces no useful findings and the dimension stays low-confidence with no refinement attempt. | After rescore (line 1991), add a conditional refinement: if `nextConfidence === "low" && usefulQueryCount <= 1 && queryPlan.queries.length >= 3`, generate refined queries that avoid the failed ones (pass `unsuccessfulQueries` to a `buildLowConfidenceRefinementPrompt`). Run a second search + rescore pass. Cap at 1 refinement per dimension. Expected trigger: 1-3 dimensions per analysis. **Files:** `src/hooks/useAnalysis.js` after line 1991 in `runLowConfidenceExtraCycle`. | Weak dimensions get a second chance. e.g. "reusability" search returns generic results → refined query targets "cross-client AI template reuse case study" and finds evidence. | 3-9 | 15-50% | P2 |
| RA-14 | improvement | Debate loop (Phase 2 → Phase 3) is reasoning-only. Critic says "I doubt the $50M claim," analyst responds "it's well-documented" — nobody checks. `api/analyst.js` has web search capability but it's not used between Phases 2 and 3. No fact-checking step exists anywhere in the pipeline. | Add verification sub-phase between Phase 2 and 3. 1. Extend `buildCriticPrompt` JSON template with `"claimsToVerify": [{"dimId", "claim", "verificationQuery"}]` (3-5 highest-impact claims). 2. New `runEvidenceVerification()` function: for each claim, call `callAnalystAPI` with `liveSearch: true` and a verification prompt that returns VERIFIED/PARTIALLY_VERIFIED/UNVERIFIED with source. 3. Inject results into Phase 3 prompt as "CLAIM VERIFICATION RESULTS" block before per-dimension critiques. 4. Add `SYS_VERIFIER` to `system.js` focused on fact-checking only. **Files:** `src/hooks/useAnalysis.js` (new function + Phase 2 output + Phase 3 prompt), `src/prompts/system.js`. | Score changes backed by fact-checked evidence. e.g. "$50M fraud savings" → verification search confirms via Mastercard earnings call → tagged VERIFIED; critic's other claim found UNVERIFIED → analyst must concede. | ~6 | ~33% | P2 |
| RA-15 | improvement | Phase 1b (`useAnalysis.js:1787-1802`) runs a single `runAnalystPass` with `liveSearch: true` for all 11 dimensions. Model allocates search budget internally — high-weight dims (roi, ai_fit, evidence) get most searches, low-weight dims (market_size, reusability) often get zero. `buildDynamicSearchPlan` (line 133-149) generates hints for top 3 dims by weight but it's a prompt hint the model can ignore. | Replace single Phase 1b call with 3 dimension-grouped calls: Group A (roi, ai_fit, evidence — 45% weight), Group B (ttv, data_readiness, feasibility, build_vs_buy — 38%), Group C (market_size, regulatory, change_mgmt, reusability — 30%). Each gets its own `buildPhase1EvidencePrompt` with `liveSearch: true` scoped to only those dims. Merge evidence payloads before the single scoring pass. Reconciliation (Phase 1c) unchanged. **Files:** `src/hooks/useAnalysis.js` in `runHybridPhase1` lines 1787-1802. | Every dimension gets minimum search coverage. e.g. market_size currently gets 0 searches while roi gets 4 → now each tier gets dedicated queries. | +2 | ~11% | P2 |
| RA-16 | improvement | Discovery (Phase 4) uses `SYS_ANALYST` for candidate generation (`useAnalysis.js:2512-2514`) and validation (`useAnalysis.js:1033-1036`). `SYS_ANALYST` frames the model as "a senior AI product analyst... assess whether a use case represents a strong custom-delivery opportunity" — wrong framing for generating strategic adjacent ideas. Candidates read like mini-assessments instead of actionable pivot suggestions. | Add `SYS_DISCOVER` to `system.js`: "You are a strategic AI portfolio advisor... identify adjacent use case opportunities that address specific weaknesses in a scored use case." Focus on narrower actionable variants, specific verticals, concrete AI approaches. Use `SYS_DISCOVER` in candidate generation (line 2512). Keep `SYS_ANALYST` for validation (line 1033) since validation IS an assessment task. **Files:** `src/prompts/system.js`, `src/hooks/useAnalysis.js` line 2512. | Better discovery candidates. e.g. current outputs are broad ("AI for supply chain") → now generates specific pivots ("demand forecasting for cold chain pharma logistics" targeting weak build_vs_buy). | 0 | 0% | P2 |

---

## [ ] FR - In-App Cost Estimator

**Problem**

PMs cannot see expected or actual analysis cost per use case directly in the app. Cost/quality trade-off discussions happen outside the product and are hard to validate against real run behavior (web-search calls, retries, low-confidence extra cycles, discovery validation passes).

**Solution**

Add an in-app cost estimator that uses run metadata (`analysisMeta`) plus configurable pricing inputs to calculate:
- estimated cost before run (based on expected pipeline steps),
- actual cost after run (based on real call counts and token usage proxies),
- incremental delta for optional modes/features (for example dual-search provider mode).

Expose this in a compact UI panel and in exports/debug logs with a clear breakdown by phase:
- analyst passes,
- critic pass,
- low-confidence targeted cycle,
- discovery + candidate validation,
- web-search tool-call costs.

---

## [ ] FR - Benchmark Regression Suite (Public Use Cases)

**Problem**

Prompt/model changes can silently degrade scoring quality. Without a fixed benchmark set, regressions are hard to detect early and quality checks rely on ad-hoc manual testing.

**Solution**

Add an automated benchmark suite with 10-20 publicly documented use cases (for example hospital readmission risk AI). For each benchmark case, store expected score ranges per dimension, confidence expectations, and key reference sources. Run the full analysis pipeline and compare outputs against expected bounds. Fail the benchmark run when dimensions drift outside tolerance, confidence collapses unexpectedly, or source quality falls below threshold.

Integrate as a repeatable script (and optional CI gate) so quality regressions are visible before release.
